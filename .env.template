# Gemini only — free intelligent models (no paid providers)
# Get a free API key at https://aistudio.google.com/apikey
GEMINI_API_KEY=your_key_here

# Allowlisted free models only (others are remapped to the default):
#   gemini-3.6-flash  (default — free, multilingual, strong accuracy)
#   gemini-3.5-flash  (free — strong on agentic/coding tasks)
#   gemini-2.5-pro    (free — deepest reasoning on free tier)
#   gemini-2.5-flash  (free — solid general free-tier model)
# Paid-only, lite, and OpenAI models are not supported.
GEMINI_MODEL=gemini-3.6-flash

# Devvit dev subreddit
DEVVIT_SUBREDDIT=r/echokeys_dev
